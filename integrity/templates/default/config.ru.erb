require 'init'

<%- if @node[:integrity][:protect_all] -%>
require 'excluding_auth'
use ExcludingAuth, /^\/github\//, "<%= @node[:integrity][:auth_user] %>", "<%= @node[:integrity][:auth_pass] %>", "Integrity"
<%- end -%>

run Integrity::App
