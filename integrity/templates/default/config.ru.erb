require 'init'

<%- if @node[:integrity][:protect_all] -%>
use Rack::Auth::Basic do |user, pass|
  user == "<%= @node[:integrity][:auth_user] %>" and pass == "<%= @node[:integrity][:auth_pass] %>"
end
<%- end -%>
run Integrity::App
